// Canonical Button lives in @plannotator/ui so the shell and the embedded
// plan/review apps share one implementation. Re-exported here to preserve the
// frontend's "@/components/ui/button" import path.
export { Button, buttonVariants } from "@plannotator/ui/components/ui/button";
